[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv>=1.1.0",
    "resend>=2.10.0",
    "schedule>=1.2.2",
    "sendgrid>=6.12.2",
]
